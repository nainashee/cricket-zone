{
  "name": "cricket-zone",
  "private": true,
  "scripts": {
    "test:unit": "cd tests/unit && node --experimental-vm-modules node_modules/.bin/jest",
    "test:api": "bru run --env dev tests/api/",
    "test:e2e": "cd tests/e2e && npx playwright test",
    "test": "npm run test:unit"
  }
}
