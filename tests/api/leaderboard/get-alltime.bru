meta {
  name: GET /leaderboard - all-time
  type: http
  seq: 2
}

get {
  url: {{base_url}}/leaderboard?category=bowling&alltime=true
  body: none
  auth: none
}

assert {
  res.status: eq 200
  res.body.leaderboard: isDefined
}
